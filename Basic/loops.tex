\subsection{Exercises: loops}

\begin{hightask}
Using a loop, print out the squares of all integers between 1 and 10.
\end{hightask}

\begin{hightask}
	Complete a code which runs your quadratic equation code from earlier, so that it will automatically solve the equations for \code{a = 2}, \code{b = 3} and the following values of \code{c} automatically: \code{-15, -10, 0, 5, 10, 15}.
\end{hightask}